{
  "name": "elk-flipbook",
  "version": "0.2.0",
  "description": "PDF flipbook widget with searchable text and OCR fallback",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "page-flip": "^2.0.7",
    "pdfjs-dist": "^4.4.168",
    "tesseract.js": "^5.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "esbuild": "^0.27.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.0",
    "vitest": "^1.0.4"
  },
  "overrides": {
    "esbuild": "^0.27.0"
  }
}
